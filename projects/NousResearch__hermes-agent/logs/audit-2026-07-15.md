# Daily audit — NousResearch/hermes-agent — 2026-07-15

## Run summary (this iteration)

- Bootstrap: gh auth OK (wesleysimplicio). Local clone at
  C:\Users\Z0059V7A\m\repos\hermes-agent (fork+origin remotes already wired,
  used in place of work/SLUG). Synced main to 5222d24f3.
- Babysit: 10 open PRs found via
  `gh pr list --repo NousResearch/hermes-agent --author wesleysimplicio --state open`
  (#65216 #65159 #65154 #64731 #64701 #64689 #64680 #64668 #59194 #59189).
  All checked with `gh pr checks` — ALL GREEN, no red CI, no fixes needed.
- **Bug caught**: #59197 and #59191 were found CLOSED (closedAt
  2026-07-15T06:34:06Z / 06:34:08Z, actor=wesleysimplicio, i.e. our own
  earlier stale-close automation) despite each carrying a COMMENTED
  maintainer review — this is exactly the guardrail violation the
  PROFILE.md notes already flagged once before (both were reopened once
  already on 2026-07-14 for the same reason). Reopened both again this run
  via `gh pr reopen 59197` and `gh pr reopen 59191` (confirmed via API:
  both succeeded). **Action needed**: the Phase 2.4 stale-close logic in
  whatever executed earlier today is not actually checking for existing
  reviews before auto-closing at STALE_CLOSE_DAYS=4 — needs a real fix,
  not just repeated manual reopens. Logged as a PLAYBOOK lesson below.
- Fresh issue scan: newest open issues as of this run (via `gh issue list
  --state open --limit 20`) topped by #65225 (KeyError
  'HERMES_KANBAN_BOARD' — kanban dispatcher mutates os.environ, racing
  dotenv's env.update(os.environ)), filed 2026-07-15T21:29:07Z, unassigned,
  0 comments. Reproducible-looking, real symptom, in a hot-ish area
  (kanban/env handling). NOT implemented this run — see decision below.
- Daily planning: no `audit-2026-07-15.md` existed before this run despite
  7 PRs already opened today (64731, 64689, 64701, 64680, 64668 predate
  this run's actions plus 64731/65154/65159/65216 also dated today) —
  i.e. substantial work already happened today via gh evidence, just
  without a corresponding dated log. Given DAILY_PR_HEALTHY=3-5 and
  DAILY_PR_TARGET=10 for this project, and 7 PRs already opened today per
  real `gh pr list` timestamps, **decision: implement ZERO new PRs this
  run** — we are already above the healthy band. #65225 is recorded as
  the top backlog candidate for the next run instead of being rushed now.
- No mechanical audit / no salvage hunt run this iteration (deferred —
  today's PR activity is already high; the reopens + stale-close bug catch
  were the highest-value actions this run).

## Merge-rate (MEASURED — `gh pr list --repo NousResearch/hermes-agent
--author wesleysimplicio --state all --json number` = 30 total;
`--state merged` = 3: #28577, #24547, #22534)

- Cumulative merge rate: 3/30 = 10%.

## Decisions

- Do not open new PRs this run (already 7 opened today per gh timestamps;
  above the 3-5 healthy band, under the 10 cap but no reason to push it).
- Next run: implement #65225 if still unclaimed/undup'd at that time, or
  the next-ranked fresh issue if it's been claimed.
