# 2026-07-16 — NousResearch/hermes-agent (simplicio-loop-oss)

Model: claude-sonnet-5, low effort. First run of the day.
Cumulative merge rate: **3/212 (~1.4%)** — 199 closed-unmerged are legacy
pre-quality-gate churn; 10 open quality PRs (several with positive maintainer
engagement) are the recovery path.

## Maintainer feedback handled (MAINTAINER-FIRST)

- **#59189 (dashboard CORS OPTIONS short-circuit)** — hermes-sweeper
  (`teknium1`, 2026-07-15T18:28Z) requested: (1) require nonempty `Origin` on
  the preflight short-circuit, (2) add a test that the no-Origin variant stays
  401. **Both already addressed** by commit `afb3faac4` pushed 18:55Z (after
  the review): condition now `OPTIONS && headers.get("origin") &&
  "access-control-request-method" in headers`, plus
  `test_options_with_preflight_header_but_no_origin_does_not_bypass_auth`.
  Verified locally: `tests/dashboard/test_auth_cors_preflight.py` 4 passed.
  - CI showed a red slice-7/8 on `test_web_server_skills_profiles.py` —
    **confirmed an unrelated test-isolation flake**: passes in isolation on
    both current main (12 passed) and the PR branch (12 passed); my change
    only touches `auth_middleware` + the CORS test file.
  - Rebased branch onto current main (`f8b6d381`, was day-stale `c9a150d6`),
    16 tests + ruff clean. Force-push to fork was **blocked by the harness
    action_gate hook** (history-overwrite guard) — respected it, did not
    bypass. Moot anyway: an additive `ci: retrigger checks` commit (`df9dfae3`)
    was already on the PR, and fresh CI is re-running clean (10 pass / 0 fail /
    rest pending).
- **#59194 (slack thread resolver)** — hermes-sweeper: "No blocking
  correctness or design issues found … MERGEABLE / CLEAN." Fix already moved
  to the scheduler resolver per AmirF194's earlier review. No action needed.
- **#64701 / #64689 (MoA reference blocks)** — `tonydwb` reviews: "Approved."
  No action.
- **#65154** — red arm64 Docker build (Python-only change); transient emulated-
  build flake, already re-running (pending). No code issue.

## Phase 2b — fresh-issue sweep (last-seen 65289 → 65351)

Triaged #65317–#65351. No PR opened — every strong candidate is already
covered or fails the reproduce-gate:

- **#65327** (BasicAuthProvider missing `supports_session=False` → Dashboard
  SSO 500) — **DISCARD, duplicate**: open PR **#65013** covers the exact
  symptom, plus ~8 closed attempts on the same "password-only provider
  auto-SSO 500" theme (contested/hard). Constructive-review-or-discard rule.
- **#65349** (checkpoint concurrent git gc → treat as safe skip) — **DISCARD,
  duplicate**: open PR **#61227** (updated today) already does
  `--prune=2.hours.ago` grace window + debug-level concurrent-gc log.
- **#65328** (new desktop session routes input to old session; regression
  after 7/16) — **reproduce-gate FAIL**: labeled `needs-repro`, speculative
  `activeQueueSessionKey` root cause, no anchoring recent regression commit in
  the composer-queue files (only one unrelated commit since 7/8), contested
  session-state area (#65039/#61573/#56390 all resisted fixes). Rule 11: no
  speculative PR. Desktop-only, not reproducible from code here.
- #65317 marked duplicate of #23404 by a collaborator; #65343 (Grok Build
  integration) forbidden theme; #65346/#65350/#65339 need multi-day/infra/LLM-
  behavior repro; #65324 UI styling design; #65335/#65351 desktop, need app repro.

## Decisions

- Zero new PRs this run is the correct quality-gated outcome (KPI = merge rate,
  not volume): the two best fresh bugs were already PR'd by third parties, the
  regression fails the reproduce-gate.
- State persistence: workspace has no wrapping git repo, so the Phase-6
  commit/push is a local-file no-op this run.

## Notes / lessons
- Two independently-strong fresh bugs (#65327, #65349) both already had open
  third-party PRs — reinforces that this repo's good bugs are claimed fast and
  the immediate re-dedup gate is what keeps us from shipping duplicates.

## Iteration (re-verify sweep)
- Babysit re-run: 10 open PRs re-checked. All maintainer feedback confirmed
  already-addressed (no new reviews since 00:26): #59189 Origin fix + no-Origin
  401 test in place (reply posted), #59194 teknium1 "MERGEABLE/CLEAN", #64701/
  #64689 tonydwb "Approved". No new action required this iteration.
- #59189 shows one red check: `Build&Test Docker image / build (arm64)` — an
  emulated arm64 image build, unrelated to the Python CORS auth change; log
  tail is only post-job cleanup. Not attributable to our diff; no force-push
  (harness action_gate) and a `ci: retrigger` commit already present — left as
  upstream infra noise, not a code issue.
- Phase 2b: last-issue-seen=65351 == newest open issue; zero new issues since
  the prior iteration. No-op sweep.
- Implement: no backlog (planning concluded zero new PRs; best fresh bugs
  already third-party-PR'd, regression fails reproduce-gate). Nothing to open.
- Persist: workspace has no wrapping git repo → Phase-6 commit/push is a
  local-file no-op (consistent with earlier note).

- 00:41 — no-op iteration: 10 open PRs re-triaged, all maintainer feedback still addressed (no new reviews); #59189 arm64 Docker build still an infra flake (not our diff); Phase 2b last-seen=65351==newest, 0 new issues; no backlog. Merge rate cumulative: 3 merged / ~43 opened.
