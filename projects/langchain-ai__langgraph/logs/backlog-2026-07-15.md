# Backlog — langchain-ai/langgraph — 2026-07-15 (ranked)

**Gate note**: every item requires maintainer assignment on its issue BEFORE a
PR can be opened (see PROFILE.md issue-link gate). Rank = attractiveness once
assignment is obtained; the gating action per item is "request assignment".

1. **#5225** — Default value of state variable not working with reducer
   function. Core `langgraph` bug, likely small & reproducible from code.
   *Next step: reproduce, then request assignment.*
2. **#2555** — Using Pydantic state with aliased fields. Known area, bounded.
   *Next step: reproduce, then request assignment.*
3. **#6412** — `ToolNode.ainvoke` freezes with `sse_read_timeout`. Higher value
   (hang bug) but likely harder to repro (needs MCP/SSE). Investigate feasibility.
4. **#5029** — Add Windows build to CI. CI-scoped, self-contained; but touches
   workflows (maintainer-sensitive). Lower priority for a newcomer.
5. **#5077** — Pandas serialization/deserialization with msgpack. Scope unclear;
   defer until 1–3 explored.

Deferred / rejected sources:
- Dep bumps (dependabot-owned), release PRs (maintainers), typo/format cleanup
  (#5021 exists to block these), multi-package changes.

**Today's decision**: reconnaissance day. No assignment requests or PRs opened
yet — investigate reproducibility of #5225/#2555 in a future run, then request
assignment on exactly one before implementing.
