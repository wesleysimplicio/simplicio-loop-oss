# Daily log — langchain-ai/langgraph — 2026-07-15

## Run 1 (Phase R reconnaissance)
- Bootstrap OK: `gh auth` = wesleysimplicio; workspace + clone clean; forked
  `wesleysimplicio/langgraph`; cloned to `work/langchain-ai__langgraph`; `fork`
  remote set; `main` synced.
- Babysit: 0 PRs of ours (open or closed) — nothing to triage.
- Phase R complete → wrote PROFILE.md, backlog-2026-07-15.md, opened-prs.md.

### Key finding — hard issue-link + assignment gate
`require_issue_link.yml` (`ENFORCE_ISSUE_LINK: true`) auto-closes any external
PR lacking `Fixes #NN` + author assigned to that issue. Blind PRs are closed by
a bot within minutes. => Assignment-first is mandatory. No PRs can be opened
until a maintainer assigns us to an approved issue.

### Benchmark (see PROFILE.md)
Merged mix ~60% dependabot, ~15% releases, rest maintainer/external fix.
External human merges rare & tiny (single package, ~+4..75 lines, terse body).

### Decision / strategy today
Reconnaissance day, newcomer gate active (0 merges). No PRs, no assignment
requests yet — deliberately choosing NOT to spam a fresh account. Next run:
reproduce top candidates (#5225, #2555), then request assignment on exactly one
before implementing.

## Counters
- PRs opened today: 0 (cap: NEWCOMER_DAILY_PR_CAP=2; healthy=1)
- Open unreviewed PRs of ours: 0 (gate: NEWCOMER_MAX_OPEN_UNREVIEWED=3)
- **Cumulative merge rate: 0/0 (no PRs opened yet)**
