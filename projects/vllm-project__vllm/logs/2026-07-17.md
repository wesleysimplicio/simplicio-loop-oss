# vllm-project/vllm — 2026-07-17

**Status: newcomer gate (0 PRs opened/merged). KPI merge rate = n/a (0 opened).**
**Outcome today: 0 PRs opened — correct quality call (all CPU-verifiable candidates already claimed; residual queue is GPU-only). Logged below.**

## Bootstrap
- `MEASURED|` gh auth ok (wesleysimplicio). Workspace clean, `git pull` up to date.
- `MEASURED|` clone synced to origin/main (fast-forward, clean tree).
- `MEASURED|` our PRs (all states) = `[]` — still 0. Nothing to babysit (Phase 2 no-op).

## Phase 2b — fresh issue scan
- Watermark advanced 48753 → 48966.
- Queue #48753..#48966 is overwhelmingly GPU/hardware-specific and out of scope per PROFILE (can't reproduce/test without GPUs): XPU (#48953,#48946), ROCm (#48941), NVFP4/SM120 (#48898,#48862,#48921), CUDA-graph/MTP spec-decode (#48848,#48894,#48840,#48503,#48749), Marlin kernels (#48905,#48904,#48895,#48896), KV-cache offload geometry (#48959,#48919), quant precision (#48492).
- CPU-verifiable candidates identified, all triaged:
  - `MEASURED|` #48853 detokenizer prompt-text leak (mid-UTF-8) — **already covered by open PR #48854** (spped2000). Dedup fail. Skip.
  - `MEASURED|` #48863 reasoning null content (include_reasoning=False + json_schema) — **already covered by open PR #48865** (Karim-siala). Dedup fail. Skip.
  - `MEASURED|` #48702 [Parser] streaming tool-call args truncated by coercion — reporter muhammadfawaz1 (established contributor) **already opened PR #48706**. Dedup fail. Skip.
  - `MEASURED|` #48931 DeepSeek-V4 tool parser leaks DSML when model omits `<｜DSML｜tool_calls>` START — **contentious design change, rejected as candidate.** Post-#45877 the parser is a state machine (`vllm/parser/deepseek_v4.py`); the fix would require a `(CONTENT, INVOKE_PREFIX)` transition, which directly breaks the existing intentional test `test_function_calls_block_is_not_accepted` (asserts an intact invoke block with a wrong/absent wrapper is NOT accepted — maintainers anchor detection on the wrapper by design). Trigger (model omitting START at 95k ctx) also not CPU-reproducible. Not a clean newcomer bugfix.

## Phase 1c — release/CI (light)
- `UNVERIFIED|` not separately queried this run; no default-branch breakage surfaced during sync (ff-only pull clean).

## Strategy (Phase 1b)
- vLLM remains a fast-moving, ultra-competitive repo: CPU-friendly bugs are claimed within hours by established contributors (today all three CPU-verifiable issues already had open PRs). Newcomer-viable, non-GPU, unclaimed supply is structurally thin here — consistent with prior daily logs and the process lesson about GPU-heavy queues.
- Correct play today: open zero PRs. Opening a competing PR on a claimed issue, or a design-contentious PR on #48931, would burn newcomer reputation for negative expected merge value.
- No maintainer feedback pending (0 open PRs of ours).

## Backlog
- See `backlog-2026-07-17.md` (empty — no surviving candidates).

## Cumulative merge rate
- 0 opened / 0 merged — no change.
