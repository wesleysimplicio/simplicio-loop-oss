# 2026-07-15 — vllm-project/vllm

## Iteration (Phase R reconnaissance + planning)
- Bootstrap OK: gh auth `wesleysimplicio`, workspace clean, `git pull` up to date.
- Forked `wesleysimplicio/vllm`; shallow blob:none clone (--shallow-since 35d) at work/vllm-project__vllm; `fork` remote added.
- **Babysit**: no open/closed/merged PRs of ours → nothing to babysit. Newcomer status.
- **Phase R**: wrote PROFILE.md. Key facts: DCO-only (no CLA), bracket-tag PR titles, template Purpose/Test Plan/Test Result, small merged-PR envelope, GPU-heavy tests.
- **Phase 1c release check**: main CI green (recent runs success), latest release v0.25.1 (2026-07-14) fresh → no breakage candidate.
- **Phase 3/3b**: contributor benchmark captured in PROFILE (hot areas: ROCm CI, LoRA, MLA/KV-cache, quant, MoE). Good-first-issues are mostly Feature/RFC/refactor.
- **Phase 4**: candidate pool seeded in backlog-2026-07-15.md; ranked; not yet dedup-confirmed/reproduced.

## Strategy today
Recon-only. Establish profile + candidate backlog. Open ZERO PRs — no CPU-reproducible
fail-before/pass-after candidate verified within budget; speculative GPU PRs violate the
mandatory-test gate and burn newcomer reputation. Correct KPI call is 0 PRs with reason logged.

## Metrics
- PRs touched today: 0 (none exist).
- PRs opened today: 0.
- **Cumulative merge rate: 0/0 (n/a) — newcomer, no PRs yet.**

## Iteration 2 (babysit + Phase 2b + candidate triage)
- **Babysit**: still 0 PRs of ours (open/closed/merged) → nothing to babysit. Newcomer.
- **Phase 2b fresh-issue scan** (watermark → last-issue-seen.md = 48747): newest issues are all
  GPU/hardware-specific — XPU (#48744 Gemma-4 MoE), ROCm (#48723 gfx1201 LDS limit), RTX 5090
  sm_120 MoE perf (#48732), GB200 CI (#48742), RFCs/features (#48747,#48743). #48745 (spurious
  EngineDeadError on graceful shutdown) is CPU-tractable asyncio but ALREADY CLAIMED
  (samikshaaagarwal requested assignment 2min after filing) → assignment gate = skip.
- **Backlog triage — all 3 candidates DISCARDED after dedup/inspection**:
  - #32588 (audio >30s timestamps): 6 linked PRs (#33564,#34060,#34461,#45063,#45115,#48225),
    reporter dr75 committed to fixing → heavily contested, fails dedup. DISCARD.
  - #34752 (--kv-cache-dtype vs kv_cache_quant_algo): many competing PRs (open #47100,#35591,
    #45582,#36312; several closed) + maintainer-tracked #33021; ModelOpt/NVFP4 = GPU-heavy, not
    CPU-verifiable. DISCARD.
  - #31624 (ModelOpt Llama-4 slow load): needs model weights, not CPU-reproducible. DISCARD.
- good-first-issues (#41230 etc.) all multi-claimed by contributors+bots.

## Strategy today (iteration 2)
No unclaimed, CPU-reproducible, uncontested candidate exists this run. vLLM is ultra-competitive
(PR#s ~48k, issues claimed in minutes). Opening a speculative/GPU PR or a duplicate of a contested
issue violates the mandatory-test + dedup + newcomer-reputation gates. Correct KPI call: **0 PRs**,
reason logged. Quality > volume.

## Metrics (iteration 2)
- PRs touched today: 0. PRs opened today: 0 (cumulative 0).
- **Cumulative merge rate: 0/0 (n/a) — newcomer, no PRs yet.**

## Next run
Re-run Phase 2b (watermark 48747) for a fresh unclaimed CPU-reproducible bug (arg/config
validation, error-message, readable logic). Avoid the contested audio/kv-cache/quant areas.
Only implement with a genuine CPU fail-before/pass-after test under the newcomer cap (2/day).

## Iteration 3 (bootstrap + babysit + Phase 2b)
- Bootstrap: `gh auth status` OK (`wesleysimplicio`), clone at work/vllm-project__vllm clean,
  fast-forwarded main (`64721312`..`1d99f0f4`, 80 files). Note: WORKSPACE
  (`.claude/skills/simplicio-loop-oss`) itself has no `.git` here — state below is written to
  disk but NOT committed/pushed this run (no git repo present at that path or any parent on this
  machine); flagging for the user rather than `git init`-ing a side effect.
- **Babysit**: `gh pr list --author "@me" --state all` → still `[]`. Nothing to babysit. Newcomer.
- **Phase 2b fresh-issue scan** (watermark 48747 → 48753): 3 new issues.
  - #48753 (Qwen3/Qwen3-Coder tool parser `strip()` corrupts whitespace in string params,
    breaks exact-match edit tools) — CPU-reproducible, well-documented, 0 comments at scan time.
    BUT the issue body itself states "I have a patch with regression tests ready ... will follow
    up with a PR" — reporter (`h0jicha`) has explicitly claimed it via "PR incoming" language
    (Phase 4 dedup guidance: treat this as a claim, same as an "I'll take this" comment). No PR
    yet (`gh search prs "48753"` empty), but implementing against it now would very likely
    produce a same-day duplicate. DISCARD (claimed by reporter intent).
  - #48752 (GLM-5.2-FP8+DSpark RPC timeout, Blackwell GB200) — comment claiming it
    (buffett0323) + GPU/multi-node hardware-specific. DISCARD (claimed + not CPU-reproducible).
  - #48749 (MRV2 MTP speculative decoding CUDA graph crash) — claimed via `/assign` comment
    (vivek41-glitch) + CUDA-graph/GPU-specific. DISCARD (claimed + not CPU-reproducible).
- Backlog (`backlog-2026-07-15.md`) already emptied in iteration 2; no survivors added this run.
- Daily planning already done today (audit/backlog exist) — not re-run.

## Strategy (iteration 3)
All 3 fresh issues since the last watermark are already spoken for (explicit claim comment,
`/assign`, or reporter-stated "PR incoming"). No unclaimed, CPU-reproducible, uncontested
candidate this run. Correct KPI call: **0 PRs**, reason logged.

## Metrics (iteration 3)
- PRs touched today: 0. PRs opened today: 0 (cumulative 0).
- **Cumulative merge rate: 0/0 (n/a) — newcomer, no PRs yet.**

## Iteration 3b (simplicio-runtime pass)
User requested driving this iteration through `simplicio-runtime` (CLI, MCP stdio ping failed —
"guidance-variant: cli" fallback per `simplicio doctor`). Used:
- `simplicio doctor --json`: overall `warning` (optional GGUF model + MCP handshake only), not
  blocking. `gh`/`git`/adapters all available.
- `simplicio runtime map --repo <CLONE> --json`: native map via `simplicio-mapper`, artifact at
  `<CLONE>/.simplicio/runtime-project-map.json`, 7 changed files since last map (matches today's
  `git pull` delta).
- `simplicio precedent init/index --repo <CLONE>`: was uninitialized (0 rows) — bootstrapped the
  FTS5 store (`sqlite-vec` not loaded, FTS-only) for future "have we seen this" recall. Empty this
  run (no prior indexed evidence for this project).
- `simplicio issue-factory discover --repo <CLONE> --json`: surfaced 20 open issues incl. 2 not in
  the earlier manual Phase 2b scan (older than the 48747 watermark but still open):
  - #48702 (Parser: streaming tool-call args truncated by schema coercion) — issue-factory said
    `unclaimed`; manual `gh issue view --json closedByPullRequestsReferences` found **PR #48706
    already open** (filed same day, ~40min after issue). DUPLICATE, discard.
  - #48667 (Idefics3/SmolVLM num_patches=0 mis-sizes pixel_values) — issue-factory said
    `unclaimed`; manual check found **PR #48760 already open** (filed ~minutes after issue).
    DUPLICATE, discard.
  - Confirmed issue-factory's `claim_status` does not check comments/linked-PRs
    (`comments_checked:false` in its own output) — it mislabeled #48745 "unclaimed" earlier too
    despite an active assignment comment. Manual dedup via `gh` stays mandatory; issue-factory
    only accelerates the fan-out. Lesson recorded in PROFILE.md.
- No new viable candidate found via this pass either. Confirms iteration 3's conclusion, now with
  a wider net (issue-factory's 20 vs Phase 2b's watermark-limited scan) and a precedent-memory
  substrate in place for future runs.

## Metrics (iteration 3b)
- PRs opened: 0 (cumulative 0/0, n/a).

## Iteration 4 (bootstrap + babysit + Phase 2b + targeted CPU-candidate search)
- Bootstrap: `gh auth status` OK (`wesleysimplicio`); clone at work/vllm-project__vllm clean
  (only untracked `.simplicio/` runtime artifact); `git pull --ff-only origin main` → up to date.
  simplicio MCP tools (issue-factory/precedent/map) still connecting this run → used plain-shell
  fallback per SKILL.md (accelerant, not a dependency). **WORKSPACE has no `.git`** (confirmed
  again: `git rev-parse` fatal) — state written to disk but NOT committable/pushable this machine;
  flagged for user, not `git init`-ing as a side effect.
- **Babysit**: `gh pr list --author "@me" --state all` → `[]`. Nothing to babysit. Newcomer.
- **Phase 2b fresh-issue scan** (watermark 48753): newest open issue is still 48753 — **no new
  issues above the watermark** this run. Re-checked the recent window; all still GPU/hardware or
  claimed (#48752 claimed by buffett0323; #48749 `/assign` vivek41-glitch; #48745 claimed by
  samikshaaagarwal + reporter; #48747 Feature; #48744/#48732/#48723/#48721 GPU/XPU/ROCm-specific).
- **#48742 investigated** (`[CI Failure]: test_score_api_queries_str_documents_text_content`, 0
  comments, unclaimed, ci-failure label): body = numerical **tolerance mismatch on GB200** running
  a cross-encoder VISION scoring test (needs GPU + model weights). NOT CPU-reproducible; prior
  iterations' "GB200 CI" tag was correct. DISCARD.
- **Targeted CPU-candidate search** (profile's preferred class — arg/CLI/validation/error-message):
  `gh search issues` on 4 term-sets + `good first issue` label scan. All hits are GPU/quant-heavy
  (kv-cache-dtype #48380/#34752, fp8 #41343, gemma4 #42110, Mamba2 #48401, DSpark) or
  feature/refactor GFIs that are multi-claimed (c6–c52 comments). No unclaimed, CPU-reproducible,
  uncontested bug this run.
- Watermark unchanged (48753 — no newer issue).

## Strategy (iteration 4)
No unclaimed, CPU-reproducible, uncontested candidate exists this run — confirmed via fresh-issue
scan, targeted bug search, and GFI-label scan. vLLM remains ultra-competitive + GPU-heavy exactly
as PROFILE describes. Opening a speculative/GPU-dependent or contested-duplicate PR would violate
the mandatory-test + dedup + newcomer-reputation gates. Correct KPI call: **0 PRs**, reason logged.
Quality > volume.

## Metrics (iteration 4)
- PRs touched today: 0. PRs opened today: 0 (cumulative 0).
- **Cumulative merge rate: 0/0 (n/a) — newcomer, no PRs yet.**
