# Backlog 2026-07-15 — vllm-project/vllm

Newcomer gate active (0 merges): cap 2 PRs/day, max 3 open unreviewed.
No release/CI breakage (main green, v0.25.1 fresh) → no priority-1 candidate.

## Candidate pool (unverified — need CPU-reproducible fail-before before any PR)
Ranked; each requires dedup + reproduction before implementation.

1. #32588 [Bug] Wrong timestamps if audio > 30s — audio/whisper transcription; likely
   pure-Python timestamp math. Best newcomer candidate IF reproducible on CPU.
2. #34752 [Bug] Improve `--kv-cache-dtype` behavior when checkpoint specifies
   `kv_cache_quant_algo` — arg/config validation; may be CPU-inspectable.
3. #31624 [Bug] ModelOpt Llama-4 checkpoints take 5+ min to load — likely needs weights; deprioritize.

Feature/RFC/refactor "good first issues" (#41230,#39479,#38425,#37753,#33267,#32335,
#32268,#31414,#31249,#30075,#29405,#29139) are heavy — deprioritized for a newcomer.

## Decision this run
Recon day. Profile written. **0 PRs opened**: no candidate has a CPU fail-before/pass-after
test verified within budget, and mandatory-test + newcomer-reputation gates make a speculative
GPU-dependent PR the wrong KPI play. Next run: attempt to reproduce #32588 on CPU; if it
reproduces with a pytest, that becomes candidate #1.
