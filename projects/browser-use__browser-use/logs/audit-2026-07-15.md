# Audit — browser-use/browser-use — 2026-07-15

Phase 3 mechanical audit. Python repo → ruff/pyright are the real signal (audit.py
scans *.py generically). Not run in depth this iteration: no clean candidate would
result (near-universal contention on open bug issues; see backlog). Deferred full
`uv run ruff check --select PLE` + `uv run pyright` scan to a run where the local
env is synced (`uv sync --all-extras --dev`) and a candidate is being implemented.

Open issue snapshot: ~314 open. Bug-labeled issues sampled (#4846, #4868, #4877,
#4970, #5017, #5041, #5108, #5132, #5188) — all OPEN; all except #5108 already have
≥1 linked/competing PR. No `help wanted` issues currently open.
