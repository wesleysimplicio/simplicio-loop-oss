# 2026-07-15 — browser-use/browser-use

## Iteration (first run — Phase R)

- Bootstrap OK: gh auth `wesleysimplicio`, workspace clean, fork exists, clone synced
  (`main`). No CLA/DCO.
- **Phase R reconnaissance done** → PROFILE.md written (identity, rules, toolchain,
  benchmark snapshot, contributors, review culture, forbidden themes, strategy).
- Babysit: no PRs of ours (0 open/closed/merged) → nothing to babysit.
- Newcomer gate ACTIVE (0 merges): cap 2 PRs/day, max 3 open unreviewed.

## Strategic reflection (Phase 1b)

- browser-use is a 104.8k-star, core-team-dominated repo. 30-day authorship is 100%
  core team (Laith, Saurav, Magnus, Shawn, Alexander). No external is a regular merger.
- Realistic external path = one small, clearly-correct, UNCONTESTED bug fix w/ repro,
  or a genuinely-needed docs fix. Merge rate >> volume here.
- **Contention is the dominant risk**: every sampled open bug issue except #5108 has
  ≥1 linked/competing PR; #5041 has 6 linked PRs + 5 claimants. Rushing a PR into a
  contested issue would burn newcomer reputation.

## Decisions

- Opened **0 PRs** today — deliberate. No clean, uncontested, locally-verifiable
  candidate; newcomer gate + reputation argue against a speculative/competing PR on
  iteration 1. This maximizes merge rate (the KPI). Logged reason satisfies the run
  promise (planning done, no safe candidate → zero PRs by design).
- #5108 (`sessions.create()` no browser without task) parked for reproduction next
  run — 0 linked PRs but needs cloud-SDK repro before committing.

## Metrics

- Cumulative merge rate: 0/0 (no PRs opened yet).
- PRs touched today: 0. PRs opened today: 0.

## Iteration 2 (same day — user requested at least 1 PR)

- #5108 checked: it's a `browser_use_sdk` (closed-source cloud SDK) issue, not
  reproducible/fixable in this OSS repo. Abandoned.
- Rechecked all fresh issues (#5236, #5228, #5216, #5195, #5188) — every single one
  already has ≥1 open competing PR. Confirms the extreme-contention finding.
- Found a genuine, previously-unreported bug via code reading (rule 11 — verified
  from code, real user-visible symptom, no pre-existing issue needed):
  `_detect_from_value_pattern` in `browser_use/agent/variable_detector.py` excluded
  `+` from the email regex's local-part class, so plus-addressed emails (e.g. Gmail
  tagging `user+tag@example.com`) were never detected as reusable `email` variables.
- Fail-before/pass-after test added and run for real (not fabricated):
  `test_detect_plus_addressed_email_from_pattern` FAILED before the fix
  (`assert None is not None`), 32/32 passed after. ruff + pyright clean on touched
  files.
- Adversarial review: 2 parallel subagents (correctness/security, code quality).
  Correctness/security: NOT REFUTED (no ReDoS, only call site is the non-security
  variable-reuse heuristic, no false positives). Code quality: REFUTED on one point
  (new test's docstring style didn't match the file's terse one-line convention) —
  fixed before push; re-ran tests, still 32/32 green.
- Dedup: searched `variable_detector email` / `plus addressed email` before AND
  immediately before opening — zero hits both times.
- Opened PR: https://github.com/browser-use/browser-use/pull/5238
  (`fix/variable-detector-plus-addressed-email`, ~11 changed lines, well inside the
  observed external-merge envelope).

## Metrics (end of day)

- PRs opened today: 1 (#5238). Cumulative merge rate: 0/1 (pending review).

## Next run

- Babysit PR #5238 (CI status, AI review bot comment, maintainer feedback).
- Consider a technical review on an active third-party PR to build reputation while
  waiting on #5238.
- Newcomer cap allows 1 more PR today if a clean, uncontested candidate surfaces.
