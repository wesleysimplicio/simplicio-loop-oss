# 2026-07-15 — home-assistant/core

## Run type: Phase R reconnaissance (first run; no PROFILE.md existed)

- Bootstrap: `gh auth` OK (`wesleysimplicio`). Fork created:
  `wesleysimplicio/core`. Default branch = `dev`.
- Our PRs in this repo: 0 (open/closed/merged). **Newcomer gate active.**
- Heavy clone into `work/` DEFERRED: repo is multi-GB and we are CLA-blocked
  from opening PRs today, so recon was done via `gh api` (contents + PR list)
  to save time/disk. Clone on the first post-CLA implementation run.

## Phase R findings → PROFILE.md written

- **CLA REQUIRED and NOT signed** for `wesleysimplicio` (root `CLA.md` +
  `home-assistant/cla` bot). **HARD STOP: zero PRs until the human signs it.**
- Benchmark snapshot: small integration-scoped PRs (bumps, bug fixes,
  diagnostics/reconfigure, quality-scale rules). Diff envelope ~+1 to +150.
- Top contributors mapped; maintainers (epenet/balloob/joostlek) separated
  from externals to imitate (oyvindwe/tr4nt0r/bkobus-bbx/amitkio).
- `good first issue`: 0 open — newcomer path is a small fix in a readable
  integration backed by an existing issue.

## Strategy today

Open ZERO PRs (recon + CLA hard stop). Escalate to user: sign the Home
Assistant CLA once for `wesleysimplicio`. No babysitting needed (no open PRs).

## Metrics

- PRs opened today: 0. PRs touched: 0.
- Cumulative merge rate: 0 merged / 0 opened = N/A (no PRs yet).

## Blockers / asks for the user

1. **Sign the Home Assistant CLA** for GitHub account `wesleysimplicio`
   (one-time). Record the date in PROFILE.md once done. Until then this loop
   stays in babysit/recon-only mode for this project.
