# Backlog — home-assistant/core — 2026-07-15

**Status: EMPTY / on hold.** Implementation is blocked by the unsigned CLA
(see PROFILE.md "Strategy"). No candidates promoted today — opening any PR
before the CLA is signed burns newcomer reputation.

When the CLA is signed, seed the backlog from (ranked):
1. Small bug fix in a single integration with an open issue + clear repro.
2. `quality_scale.yaml` `todo` rule completion in one integration.
3. Safe dependency bump where a newer release closes a referenced issue.

Newcomer caps until first merge: `NEWCOMER_DAILY_PR_CAP=2`,
`NEWCOMER_MAX_OPEN_UNREVIEWED=3`; healthy default 1–2/day.
