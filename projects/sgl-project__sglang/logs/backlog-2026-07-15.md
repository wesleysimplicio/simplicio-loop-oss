# Backlog 2026-07-15 — sgl-project/sglang

Recon day. Newcomer gate active (0 merges → cap 2/day, healthy 1/day).
No PR opened this iteration (reasons in daily log). Candidate LEADS to verify next run
(each still needs Phase 4 dedup + confirm-on-main + a no-GPU verification path):

1. [lead] Docs/cookbook accuracy fixes — lowest-risk first-merge path (matches merged
   externals like #31258). Scan docs_new/ & cookbook for concrete broken links/commands.
2. [lead] CI/test import-path or pure-python correctness fix tied to an OPEN issue
   (model of #31199 +1/-1, #31234 +14/-1). Requires an issue with a code-readable repro.
3. [lead] good-first-issue #18427 "remove duplicated contents in SGLang Docs/Cookbook"
   — docs-only, no GPU. Verify it's still open/unclaimed and scoped small before taking.

REJECTED this run:
- ruff F401 sweep (49 findings): mass mechanical lint cleanup = forbidden theme; all in
  re-export __init__.py shims (intentional).
- GPU-dependent runtime bug fixes: cannot reproduce/verify locally (no GPU) → would
  violate the no-fabrication + reproduce-first rules.
- ModelRunner/KV-cache refactors: core maintainers actively churning; high conflict risk.
