# Backlog — sgl-project/sglang — 2026-07-17

**No approved candidates.** Every strong, CPU-verifiable, newcomer-viable issue in
the fresh sweep (#31352→#31600) already carries a linked fix PR (claimed within the
minutes-wide window this repo is known for — PROFILE lesson confirmed). The one
genuinely-unclaimed candidate is not safely verifiable without a GPU/CI.

## Evaluated & rejected
| Issue | Title | Verdict |
|-------|-------|---------|
| #31597 | Unvalidated request fields crash server (topk/scatter OOB) | CLAIMED — reporter's own PRs #31567/#31561 linked |
| #31598 | Streaming detokenization duplicates a char (sent_offset retreat) | CLAIMED — reporter PR #31564 |
| #31599 | Spec decode max_new_tokens cap masks in-budget stop string | CLAIMED — reporter PR #31569 |
| #31528 | XPU MoE topk_softmax() TypeError (6 args vs 4) | CLAIMED — PR #31536 (also #31130) |
| #31533 | xgrammar reqs never terminate when tokenizer eos missing | CLAIMED — PR #31534 |
| #31568 | Unused tl.constexpr params → Triton cache-key pollution | UNCLAIMED but REJECTED: touches GPU/Triton + XPU FLA kernels; removes a kernel bounds guard (`if pid >= N`) we cannot verify safe without a GPU/CI (wrong = OOB memory regression). No fail-before/pass-after runtime test path. Unsafe as a newcomer first PR. |

GPU/ROCm/XPU/kernel bug issues (#31600, #31594, #31588, #31490, #31475, …) are out of
our lane (no hardware, can't reproduce/verify) per PROFILE "Forbidden themes".
