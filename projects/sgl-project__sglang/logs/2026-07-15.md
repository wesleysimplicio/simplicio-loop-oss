# 2026-07-15 — sgl-project/sglang (first run: Phase R + planning)

## Bootstrap
- gh auth OK (wesleysimplicio). Workspace clean, pulled. Fork exists: wesleysimplicio/sglang.
- Clone fixed on Windows via `git config core.longpaths true` (long triton-config filenames).
- Default branch: main, synced.

## Phase R — reconnaissance
- Wrote PROFILE.md. Key findings: no CLA, no DCO; heavy merge process (oncall ping +
  CODEOWNERS + authorized-only CI trigger) → newcomer PRs sit until a maintainer engages.
- Very high-velocity, maintainer-driven repo. External-viable PRs are small & focused
  (≤ ~60 lines): CI/test-path fixes, docs/cookbook, issue-linked correctness fixes.

## Phase 1 — closures
- None (0 PRs opened by us ever). Newcomer gate active.

## Phase 1b — strategy today
- First-run recon + planning. Chose to open ZERO PRs: (1) recon day, (2) ruff findings
  are forbidden mechanical cleanup / intentional shims, (3) runtime bugs need a GPU we
  don't have (reproduce-first + no-fabrication rules), (4) no small non-GPU candidate
  confidently deduped yet. Built a ranked backlog of leads for the next iteration.

## Phase 1c — release/CI
- main CI healthy; no branch breakage → no priority fix.

## Phase 2 — babysit
- No open PRs of ours. Nothing to babysit.

## Phase 3 / 3b — audit + benchmark
- Audit: audit-2026-07-15.md (49 ruff F401 — not actionable). Benchmark captured in PROFILE.

## Iteration 2 (later same day) — babysit + Phase 2b fresh-issue scan
- Bootstrap OK: gh auth wesleysimplicio; clone clean, main synced. NOTE: workspace dir
  (.claude/skills/simplicio-loop-oss) is NOT a git repo in this host → state persists as
  local files only; no `git push` possible this run (logged, not forced).
- Babysit: still 0 open/closed/merged PRs of ours. Nothing to babysit. Newcomer gate active.
- Phase 2b: scanned 15 newest issues (up to #31337; wrote last-issue-seen.md=31337).
  Triage of the non-GPU-looking candidates:
  - #31240 (RunAI streamer model_name URI not resolved): ALREADY FIXED on main by #31211
    (Sam Shleifer, merged 2026-07-15T07:21Z, ~7h after the issue was filed 00:28Z) AND
    already has open PR #31288 (@sam-shridhar1950f). Dead end — a PR would double-duplicate.
  - #31271 (gpt-oss-20b merged-LoRA tokenizer stays TokenizersBackend): needs the user's
    specific merged model to reproduce; no GPU/model locally → speculative, cannot verify
    → skip (reproduce-first + no-fabrication).
  - #18427 (remove duplicated docs/cookbook contents): broad/subjective, 4-comment
    discussion, close to forbidden mass-cleanup theme → not a small first-PR target.
- All other fresh issues are GPU/hardware-specific bugs (AMD/ROCm, MoE/EP, MLA, MTP
  speculative decode) — unreproducible here per profile.

## Iteration 3 (later same day) — babysit + Phase 2b fresh-issue scan + backlog revisit
- Bootstrap OK: gh auth wesleysimplicio; clone `work/sgl-project__sglang` clean, fast-forwarded
  main to origin (pulled 3 files incl. server_args.py / jit_kernel runtime.cuh churn).
- Babysit: `gh pr list --repo sgl-project/sglang --author "@me" --state open` → `[]`.
  Still 0 open PRs of ours. Nothing to babysit. Newcomer gate active (0 merges).
- Phase 2b: scanned 20 newest issues (createdAt desc). Only 3 numbered above last-issue-seen
  (31337): #31352, #31350, #31347 (#31336 and below already seen/triaged iteration 2).
  - #31352 [Docs] Installation mentions sm75 compatibility — reporter already opened
    PR #31052 fixing this themselves ("Raised a PR to revise install.mdx #31052"). Claimed
    → skip (would duplicate).
  - #31350 [Bug][AMD] Qwen3.5 dp-attention Triton kernel HIP "invalid configuration
    argument" (zero-sized grid) — AMD/ROCm/HIP hardware bug, needs GPU to verify. Skip
    per no-GPU rule.
  - #31347 JIT hadamard/DSA-indexer kernel compile race on shared NFS tvm-ffi cache
    (ESTALE on multi-node cold start) — read the report in full. Root cause plausible from
    code description (`python/sglang/jit_kernel/utils.py:load_jit` writes to a shared
    NFS build_directory with node-local flock, no cross-node coordination; tvm-ffi links
    in place rather than temp-then-rename). A real fix here is a nontrivial concurrency/
    build-system change (unique per-node build dirs or atomic rename) that cannot be
    exercised end-to-end without an actual multi-node NFS rig to prove it doesn't just move
    the race — verification would be code-reading-only, not a real fail-before/pass-after
    test. Given the newcomer gate and mandatory-test rule, declined to implement; too risky
    to claim "verified" for a concurrency fix with zero repro capability. Left as a backlog
    lead (not filed) rather than a bad first PR.
  - #31336 (AMD MoE assertion) — GPU-only, already below last-issue-seen threshold, skipped.
- Backlog revisit: #18427 (remove duplicated docs/cookbook contents) — re-checked via
  `gh issue view 18427`. Confirmed actively claimed/in-progress: comments from
  JingwenGu0829, Sahib12spoke, and collaborator zhaochenyang20 linking merged-adjacent work
  (sgl-cookbook PR #141, sglang PR #18432), plus a 2026-05-04 comment from prakashkagitha
  (cookbook PR #23798) asking if more work remains. Not a fresh/unclaimed candidate —
  removed as a viable lead going forward.
  Also checked "good first issue" label list: top issues (#28808 refactor, #27214 model
  support) all have multiple claim comments already. No unclaimed small non-GPU candidate.
- Decision: no qualifying candidate survived triage this iteration (all fresh issues were
  either already claimed, GPU-only, or too risky to verify without a real repro
  environment). Correctly opened 0 PRs — logging why per playbook rule 6, not forcing a
  speculative PR.
- last-issue-seen.md updated: 31337 → 31352 (highest issue number observed this run).

## Metrics
- Opened today: 0 (3 iterations). Cumulative: 0 opened / 0 merged. Merge rate: n/a.
- Strategy today: patient quality-first under newcomer gate; no safe/unclaimed/non-GPU/
  not-already-fixed/verifiable candidate surfaced across any iteration → correctly opened
  0 PRs. Healthy default = 1/day; still within cap, no pressure to force a PR.
