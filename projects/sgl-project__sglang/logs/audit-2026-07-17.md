# Audit — sgl-project/sglang — 2026-07-17

- `ruff check --select F821 python/sglang/` → All checks passed! (no undefined names)
- No genuine correctness candidate surfaced. Mass mechanical cleanup is forbidden here.
- Mixed Python/Rust/C++ repo; generic scripts/audit.py degrades to SKIP for non-.py — PROFILE
  "Audit commands" are the real audit. Only the ruff F821 gate was run this planning day (cheap,
  and it is the one that catches real bugs); no candidate to justify the heavier Rust/clang passes.
