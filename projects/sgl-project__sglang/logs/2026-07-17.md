# 2026-07-17 — sgl-project/sglang (planning day)

Newcomer gate active (0 merges). Caps: NEWCOMER_DAILY_PR_CAP=2, NEWCOMER_MAX_OPEN_UNREVIEWED=3.
simplicio-runtime: MCP tools not probed this run (plain-shell fallback used throughout).

## Phase 0 — sync
- MEASURED| Workspace `git pull --ff-only`: already up to date.
- MEASURED| Clone on `main`, clean tree, `pull --ff-only origin main` fast-forwarded.
- MEASURED| `gh auth status` reports keyring token invalid, BUT real API calls succeed
  (`gh api user` → wesleysimplicio; `gh pr list --author @me` → `[]`). gh functional; not a blocker.

## Phase 2 — babysit
- MEASURED| `gh pr list --author @me --state open` → `[]`. Zero open PRs of ours; nothing to babysit.

## Phase 2b — fresh-issue scan (watermark 31352 → 31600)
Newest 15 open issues reviewed. Triage outcome:
- MEASURED| #31597 (unvalidated request fields / OOB topk+scatter DoS), #31598 (streaming detok
  duplicates a char), #31599 (spec-decode max_new_tokens masks stop string) — all by @v-code01,
  each filed WITH its own open fix PR: #31561, #31564, #31567, #31569. **CLAIMED → discard (dedup).**
  These were the only strong CPU-verifiable pure-Python bugs in the queue; a contributor is
  batch-filing issue+PR pairs faster than the loop cadence can catch.
- MEASURED| #31533 (xgrammar requests never terminate when tokenizer eos missing from config
  eos_token_id) by @qifanchriswu001 — already linked to open PR #31534. **CLAIMED → discard.**
- UNVERIFIED| Remainder are GPU/hardware-specific — #31600 (glm hicache dram cluster),
  #31594 (AMD ROCm MoRI), #31588 (EAGLE KV OOM), #31578 (Blackwell SM120), #31570 (NPU CI),
  #31568 (Triton constexpr cache-key), #31545 (ROCm MI355X), #31528 (XPU MoE topk_softmax),
  #31505/#31504/#31490 (hicache/FP8/GSM8K on GPU). Not reproducible/verifiable without a GPU →
  out of lane per PROFILE forbidden themes.
- last-issue-seen updated to 31600.

## Phase 1c — release / CI health
- MEASURED| Latest release v0.5.15.post1 (2026-07-14) — healthy.
- MEASURED| `gh run list --branch main` shows only bot/slash-command + "PR States" runs
  (success/skipped/queued). No broken default-branch CI to jump the queue.

## Phase 3 — mechanical audit
- MEASURED| `ruff check --select F821 python/sglang/` → "All checks passed!" — no undefined-name bug.
- Mass mechanical cleanup (typo/noqa/format) is FORBIDDEN here; audit surfaced no genuine correctness bug.

## Phase 3b — benchmark
- Snapshot in PROFILE.md is dated 2026-07-15 (2 days old) and still accurate: external-viable PRs
  are tiny CI/test-path/docs fixes tied to an issue; large diffs are maintainer refactors. No divergence
  observed today → not re-dating.

## Phase 4 — dedup + ranking
- MEASURED| Zero survivors. Every strong candidate is already covered by a third-party open PR;
  GPU issues are out of lane; audit clean.

## Decision
- **Open 0 PRs today.** Correct quality-first outcome: no unclaimed, CPU-verifiable, reproducible
  candidate exists. Opening a duplicate of @v-code01's / @qifanchriswu001's PRs, or a speculative
  GPU fix we can't verify, would burn newcomer reputation for zero merge-rate gain.
- Strategy going forward unchanged: patient, quality-first; first merge to come from a small
  CI/test-path/docs-accuracy fix tied to an unclaimed issue (≤ ~60 lines). Consider a constructive
  technical review on a third-party PR as a reputation-builder if a good one appears.

## Cumulative merge rate
- 0 opened / 0 merged (newcomer). No change.

## Later iteration (re-verify, same day)
- MEASURED| Re-ran bootstrap + Phase 2 + Phase 2b. Trees clean, `main` up to date,
  `gh pr list --author @me` still `[]`. No new issues past #31600; the strong
  candidates (#31597/#31598/#31599/#31528/#31533) remain claimed with linked fix PRs
  (re-confirmed #31528→#31536, #31533→#31534 this run). #31568 stays rejected
  (GPU/Triton, unverifiable, bounds-guard removal risk). No change → 0 PRs.
- Added backlog-2026-07-17.md capturing the evaluated/rejected table for the record.
