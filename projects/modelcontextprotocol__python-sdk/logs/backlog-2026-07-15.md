# Backlog — modelcontextprotocol/python-sdk — 2026-07-15

**Autonomous filing is BLOCKED here (policy — see PROFILE.md).** This is a
HUMAN shortlist: issues a human could personally claim (comment to get
assigned + disclose AI use + own the change), Otis-sized (one focused bug,
low-tens of lines, issue-linked). NOT to be filed by the loop.

## `ready for work` candidates (maintainer-approved; realistic for an external)
1. #2663 — FastMCP.run(transport="stdio") noisy traceback on KeyboardInterrupt instead of clean exit — likely small signal-handling fix.
2. #2454 — stdio_client crashes on malformed UTF-8 from child stdout instead of surfacing parse error — small, testable decode-error path.
3. #2578 — OAuth token refresh sends RFC 8707 resource param Entra ID v2.0 rejects — narrow conditional-param fix (verify reproducibility first).
4. #2655 — Streamable HTTP server drops in-flight request when client reuses a JSON-RPC id — needs care; verify scope.

## `good first issue` (newcomer-friendly; confirm still current on v2 main)
- #1933 — transport="stdio" closes real stdio → ValueError after server exits.
- #348 — no way to set isError=True for arbitrary tool result content.

Dedup status: NOT re-deduped for filing (we are not filing). A human must
re-check assignment/open-PRs before starting any of these.
