# Backlog — pydantic/pydantic — 2026-07-15 (ranked)

Newcomer gate: cap 2 PRs/day, ≤3 open unreviewed, until first merge.
Re-dedup against OPEN PRs immediately before opening anything.

1. **Broken/stale doc links & examples hunt** (docs, exempt from issue-first).
   Proven-merged external theme (Yadidiah-k #13375, lkk7 #13395, dhruvatr #13400).
   Action next run: `make docs` link-check / grep docs for dead anchors & wrong
   code in examples; open ONE fix only if a genuine, verifiable error is found.
   Risk: low. Skip if only cosmetic (see closed #13397).

2. **#13422** `to_json`: `timedelta_mode`/`temporal_mode` docs vs behavior
   (label: documentation, unassigned). Evaluate whether the fix is a docs
   correction (safe, issue-first: comment + get assigned) vs a core-behavior
   change (likely pydantic-core → out of scope here). Verify layer first.

3. **#13327** Inconsistent serialization of non-Pydantic models between union
   and single type (bug V2, unassigned). Reproduce in Python layer; if it lands
   in pydantic-core, post evidence comment instead of a PR. Requires assignment.

4. Re-check `good first issue` / `help wanted` each run (none open today).

Rejected / avoid:
- Mass mechanical edits (stacklevel/typo/lint batches) — #13396 closed.
- Any issue fix without prior assignment — auto-closed.
- Duplicating open external fix PRs (13417/13426/13429/13433 cluster on
  numeric/temporal serialization — check before touching that area).
